# Google Calendar OAuth Credentials
# Path to your Google OAuth credentials JSON file
# Download from Google Cloud Console (see SETUP_GOOGLE_OAUTH.md)
GOOGLE_OAUTH_CREDENTIALS=/path/to/your/credentials.json

# Optional: Custom token storage location
# GOOGLE_CALENDAR_MCP_TOKEN_PATH=/path/to/token.json